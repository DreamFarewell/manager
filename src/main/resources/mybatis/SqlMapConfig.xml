<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 全局setting配置 根据需要添加-->
	
	<!-- 别名 -->
	<typeAliases>
		<package name="cn.com.employee.po"/>
	</typeAliases>
	<!-- 加载映射文件
	由于使用spring和Mybatis的整合包进行mapper扫描，这是不需要配置了
	必须 遵循mapper.xml和mapper.java文件同名且在一个目录
	 -->
	<!-- <mappers>
		<mapper resource="sqlmap/User.xml" /> 
		批量加载映射文件
		和spring整合后，使用mapper扫描器，这里不需要配置了
		<package name="cn.com.ssm.mapper" /> 
	</mappers>-->
</configuration>